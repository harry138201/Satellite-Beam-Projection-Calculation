# 卫星波束投影计算
本项目可以精确计算卫星波束在地球表面的覆盖区域，支持任意星下点位置和波束指向，输出波束边界经纬度坐标和覆盖面积。不同于传统方法，在计算时直接将波束近似为标准圆形，本项目采样了波束的真实覆盖区域。

## 项目计划

- 单个波束的精确绘制
- 多个波束的排布方式（待完成）
- ......


## ✨ 核心特性

- **精确坐标变换**：直接构造正交旋转矩阵，保证坐标轴与地理北/东方向对齐，解决了传统方法旋转错误和方位角偏移问题
- **灵活输入方式**：支持偏移角度（极角+方位角）或直接输入波束中心点经纬度
- **精确面积计算**：使用高斯-博内定理计算球面多边形面积
- **可视化展示**：提供2D地理投影
- **通用适配**：支持任意轨道高度，不限于地球同步轨道

## 🔧 实现原理

**坐标系设计**：直接构造正交旋转矩阵，保证：
- 局部x轴 → 星下点切平面**地理北方向**
- 局部y轴 → 星下点切平面**地理东方向**
- 局部z轴 → 地心指向星下点方向

**计算流程**：
1. 在局部坐标系中求解波束圆锥与地球球面的交点
2. 通过旋转变换转换到全球地心坐标系
3. 转换为地理经纬度坐标
4. 应用高斯-博内定理计算覆盖面积

## 📦 安装依赖

```bash
pip install numpy matplotlib
```

## 🚀 快速开始

### 交互式运行

```bash
python beam_projection.py
```

按提示输入：卫星轨道高度、星下点经纬度、波束半顶角，选择波束中心点设置方式。

### 模块调用

```python
from beam_projection import calculate_beam_boundary, calculate_beam_area, H_GEO

# 计算波束边界（输入波束中心点经纬度）
boundary = calculate_beam_boundary(
    satellite_height=H_GEO,
    subpoint_lat=39.9,
    subpoint_lon=116.4,
    beam_half_angle=2.0,
    beam_center_lat=39.9,
    beam_center_lon=116.4
)

# 计算覆盖面积
area = calculate_beam_area(boundary)
print(f"覆盖面积: {area:.0f} km²")
```

`boundary` 为 `(n_points, 2)` numpy 数组，每行存储一个边界点的 `(lat, lon)`。

## 📊 计算示例

| 示例 | 参数 | 结果 |
|------|------|------|
| 中心点波束 | GEO轨道，星下点北京(39.9°N, 116.4°E)，波束半角2.0° | 覆盖面积 **4,985,051 km²**，范围：28.6°N~51.3°N, 101.6°E~131.2°E |
| 北偏移波束 | GEO轨道，星下点北京，波束中心44.9°N，半角1.0° | 覆盖面积 **1,238,444 km²**，中心点误差 < 0.1° |

## 🔧 问题修复

修复了传统实现中的两个关键错误：

1. **Rodrigues旋转公式系数错误**：归一化旋转轴后丢失 sin(θ) 系数，导致旋转矩阵计算错误
2. **坐标轴方向未对齐**：仅保证z轴指向星下点，x/y轴未对齐地理方向，导致方位角偏移

**解决方案**：放弃Rodrigues旋转，直接构造正交旋转矩阵，三列分别对应北、东、星下点方向，坐标系完全对齐，计算结果准确。

## 📄 许可证

MIT License
